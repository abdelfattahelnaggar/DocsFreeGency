import 'package:flutter/material.dart';

class AddProjectToPortfolio extends StatelessWidget {
  const AddProjectToPortfolio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Project to Portfolio'),
      ),
    );
  }
}
