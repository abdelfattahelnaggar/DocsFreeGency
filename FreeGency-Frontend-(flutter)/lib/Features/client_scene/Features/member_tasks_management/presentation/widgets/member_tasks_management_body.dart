import 'package:flutter/material.dart';

class MemberTasksManagementBody extends StatelessWidget {
  const MemberTasksManagementBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Member Tasks Management'),
      ),
    );
  }
}
